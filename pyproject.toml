[project]
name = "discord-logger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "discord-py-self>=2.0.1",
    "textual>=0.30.0",
    "python-dotenv>=1.0.0",
]
